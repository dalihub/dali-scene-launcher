SET(SCENE_LAUNCHER_SRC_DIR ${ROOT_SRC_DIR}/scene-launcher)

AUX_SOURCE_DIRECTORY(${SCENE_LAUNCHER_SRC_DIR} SCENE_LAUNCHER_SRCS)

SET(SCENE_LAUNCHER_SRCS
  ${SCENE_LAUNCHER_SRCS}
)

ADD_EXECUTABLE(${PROJECT_NAME} ${SCENE_LAUNCHER_SRCS})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${REQUIRED_PKGS_LDFLAGS} -pie)

INSTALL(TARGETS ${PROJECT_NAME} DESTINATION ${BINDIR})

